# Dokploy deployment (copy to .env and fill in — never commit .env)
DOKPLOY_URL=https://your-dokploy-server.com
DOKPLOY_API_KEY=your-api-key-from-dokploy-settings
# Optional: GitHub integration ID from Dokploy (Settings -> Git)
DOKPLOY_GITHUB_ID=
